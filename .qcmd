# .qcmd example file

echo "This is qcmd ..." && sleep 1 ␍

git commit -a && git push && git deploy
go install qcmd.go

Go Tasks:
    go run qcmd.go
    go install qcmd.go
    go get -u
    go get -u ./...
    go mod tidy

Git:
    git commit -a && git push
    git commit -a
    Git Subcmds:
        git push ␍
        git pull  ␍
        git deploy ␍

---
Edit .qcmd: $EDITOR .qcmd
---

ls -lart
uname -a
Print current directory: pwd ␍
